[package]
name = "ringhopper-definitions"
authors = ["Snowy Mouse"]
version = "0.2.0"
description = "Engine and tag structure definitions for Ringhopper"
license = "GPL-3.0-only"
edition = "2021"

[dependencies]
serde_json = "1.0.108"
