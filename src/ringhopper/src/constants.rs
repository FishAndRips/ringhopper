/// Number of physics tick per second.
pub const TICK_RATE: f64 = 30.0;

/// Number of seconds per physics tick.
///
/// This is the inverse of [`TICK_RATE`].
pub const TICK_RATE_RECIPROCOL: f64 = 1.0 / TICK_RATE;
